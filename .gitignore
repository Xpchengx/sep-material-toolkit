# ============================================================================
#  白名单式忽略 —— 根目录只放行「本项目自己的文件」
# ----------------------------------------------------------------------------
#  为什么反过来写：这个目录里同时在放别的任务的文件（编辑脚本、导出文本等），
#  用黑名单列不胜列，还得每次补一行 —— 漏一次就可能把不相干内容推到公开仓库。
#  白名单是「默认拒绝」，新出现的无关文件自动被挡住。
#  本仓库只保存工具代码，不保存任何业务数据。
# ============================================================================

/*
!/.gitignore
!/README.md
!/index.html
!/manifest.webmanifest
!/sw.js
!/xlsx-lite.js
!/wb-merge.js
!/wb-ui.js
!/wb_sync_agent.py
!/wb-sync.config.example.json
!/icons/
!/docs/
!/material-system/
!/skills/

# ---------- 台账与明细数据 ----------
*.csv
!docs/samples/*.csv
/material-system/out/
/material-system/src/*.xlsx

# ---------- 生成的数据文件（绝不入库） ----------
workbench-data.js
workbench-data.json
# wb-meta 含 303 条型号映射（等于物料目录）与安全库存，属业务数据
wb-meta.js
wb-meta.json

# ---------- 本机配置（含真实台账路径与集成商名） ----------
wb-sync.config.json
/material-system/src/sources.json
/material-system/src/suppliers.json

# ---------- 交付物与参考目录（含真实内容） ----------
/output/
/.tmp/
/material-system/data/
/material-system/scratch/

# ---------- 本地记忆与运行时（含单位/人名等内部信息） ----------
/.workbuddy/
/.claude/
.DS_Store
Thumbs.db

# ---------- 依赖与临时文件 ----------
node_modules/
package.json
package-lock.json
.commitmsg.txt
__pycache__/
*.pyc
*.log
*.tmp
~$*
.venv/
venv/

# ---------- 编辑器 ----------
.idea/
.vscode/
*.swp

# ---------- 根目录临时脚本（下划线前缀，含本机路径） ----------
/_*

# ---------- 其它工具留下的隐藏参考目录 ----------
/output/.*ref/
/output/.*/
